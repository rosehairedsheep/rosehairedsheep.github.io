---
import BlogPost from "./blogpost.astro"

const { title, date } = Astro.props
---

<BlogPost>
	<h2>{title}</h2>
	<div class="post-date">{date.toString().slice(8,10)}.{date.toString().slice(5,7)}.{date.toString().slice(0,4)}</div>
	<hr>
	<slot />
</BlogPost>